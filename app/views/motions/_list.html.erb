<% @motions.each do |motion| %>
  <%= render motion %>
  <div class="row-spacer"></div>
<% end %>

<div class="row">
 <!-- /col-lg-6 -->
  <div class="col-lg-12">
    <%= paginate @motions, :remote => true %>
  </div>
 </div>