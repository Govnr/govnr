<p id="notice"><%= notice %></p>

<h1>Listing Groups</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @groups.each do |group| %>
      <%= render group %>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Group', new_group_path %>
