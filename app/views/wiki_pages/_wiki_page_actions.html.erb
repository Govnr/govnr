
 <%= link_to( wt( 'All pages' ), wiki_all_path, :class => "btn btn-default btn-xs" ) %>
 <%= link_to( wt( 'History' ), wiki_page_history_path(@page), :class => "btn btn-default btn-xs" ) %>
 <%= link_to( wt( 'Edit' ),  wiki_page_edit_path(@page), :class => "btn btn-default btn-xs" ) if edit_allowed?  %>
 <%= link_to( wt( 'Destroy' ), wiki_page_path(@page), :method => :delete, :confirm => wt( "Destroyed page can't be restored. Are you sure?" ) ) if destroy_allowed? %>


