<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'chart/Chart.bundle.min', 'data-turbolinks-track' => true %>
    <!--[if lt IE 9]>
        <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
        <script src="https://oss.maxcdn.com/libs/respond.js/1.4.2/respond.min.js"></script>
    <![endif]-->
    <%= csrf_meta_tags %>
    <title>gov.pan</title>



</head>

<body>

    <div id="wrapper">

        <!-- Navigation -->
        <nav class="navbar navbar-default navbar-static-top" role="navigation" style="margin-bottom: 0">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <div class="header-logo">
                <a class="navbar-brand" href="/"> <span><%= image_tag("Gov.Pan.logo.png")  %></span><span class="navbar-title">Gov.Pan<span></a>
                </div>
            </div>
            <!-- /.navbar-header -->
           <div class="navbar-right">
            <ul class="nav navbar-top-links navbar-right">
             <% if user_signed_in? %>
                <%= render partial: "user/messages" %>
                <%= render partial: "user/active_votes" %>
                <%= render partial: "user/notifications" %>
             <% end %>
                <%= render partial: "user/user_menu" %>
            </ul>
            </div>
            <!-- /.navbar-top-links -->
            <% if user_signed_in? %>
               <%= render partial: "user/main_menu" %>
            <% else %>
                <%= render partial: "user/guest_menu" %>
            <% end %>
        </nav>
        
        <div id="page-wrapper">
        <div id="flash-messages">
            <%= render partial:'layouts/messages' %>
        </div>
            <%= breadcrumbs pretext: "", separator: " &rsaquo; " %>
            <%= yield %>
        </div>
    </div>

      <%= javascript_include_tag 'metis_menu/metisMenu.min', 'data-turbolinks-track' => true %>
         <%= javascript_include_tag 'sb-admin-2/sb-admin-2', 'data-turbolinks-track' => true %>

</body>
</html>