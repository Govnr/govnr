<% @groups.each do |group| %>
  <%= render group %>
  <div class="row-spacer"></div>
<% end %>

<div class="row">
 <!-- /col-lg-6 -->
  <div class="col-lg-12">
    <%= paginate @groups, :remote => true %>
  </div>
 </div>